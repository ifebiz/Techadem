[build]
  command = "corepack enable && pnpm install && pnpm --filter @workspace/techadem run build"
  publish = "artifacts/techadem/dist/public"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
